# 🔧 Configuration de production pour déploiement Hostinger
# Copiez ce fichier vers .env.production et ajustez les valeurs selon vos besoins

# === CONFIGURATION SSH ===
# Nom d'utilisateur Hostinger
SSH_USER=u403023291

# Adresse IP ou nom de domaine du serveur
SSH_HOST=147.79.98.140

# Port SSH (Hostinger utilise généralement 65002)
SSH_PORT=65002

# === CONFIGURATION SERVEUR ===
# Chemin vers le dossier web sur le serveur
REMOTE_PATH=/home/u403023291/domains/glp1-france.fr/public_html

# URL du site web final
SITE_URL=https://glp1-france.fr

# === CONFIGURATION BUILD ===
# Dossier de build local (généré par npm run build)
LOCAL_BUILD_DIR=dist

# === CONFIGURATION BACKUP ===
# Nombre de jours de conservation des backups distants
BACKUP_KEEP_DAYS=7

# === CONFIGURATION RSYNC ===
# Options rsync supplémentaires (optionnel)
# RSYNC_EXTRA_OPTS="--bandwidth=1000"

# === CONFIGURATION DE DÉPLOIEMENT ===
# Timeout pour les connexions SSH (en secondes)
SSH_TIMEOUT=30

# Nombre maximum de tentatives de reconnexion
MAX_RETRIES=3

# === NOTIFICATIONS (optionnel) ===
# URL webhook Discord/Slack pour notifications (laissez vide si non utilisé)
# WEBHOOK_URL=""

# Email pour notifications d'erreur (laissez vide si non utilisé)
# NOTIFICATION_EMAIL=""

# === SÉCURITÉ ===
# Mode strict : arrêt en cas d'erreur (true/false)
STRICT_MODE=true

# Vérification de l'intégrité des fichiers après upload (true/false)
VERIFY_INTEGRITY=true

# === PERFORMANCE ===
# Compression lors du transfert (true/false)
USE_COMPRESSION=true

# Transfert en parallèle (true/false)
PARALLEL_TRANSFER=false

# === MAINTENANCE ===
# Mode maintenance : crée une page de maintenance pendant le déploiement (true/false)
MAINTENANCE_MODE=false

# Délai d'attente avant vérification du site (en secondes)
VERIFY_DELAY=5

# === ENVIRONNEMENT ASTRO ===
# Variables spécifiques à l'environnement de production
NODE_ENV=production
ASTRO_TELEMETRY_DISABLED=1

# === NOTES ===
# 1. Ne commitez JAMAIS ce fichier avec des vraies credentials
# 2. Utilisez des clés SSH plutôt que des mots de passe
# 3. Testez toujours avec --dry-run avant un déploiement réel
# 4. Gardez des backups réguliers de votre base de données si applicable
