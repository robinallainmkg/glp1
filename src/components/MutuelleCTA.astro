---
// DISABLED — Politique Sinocare-only (12/05/2026, décision user).
// Le CTA "guide remboursement mutuelle" est masqué (distraction non monétisable).
interface Props {
  context?: string;
}
const _props = Astro.props as Props;
void _props;
---
