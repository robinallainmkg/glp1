---
// DISABLED — Politique Sinocare-only (12/05/2026, décision user).
export interface AffiliateProduct { productName: string; brand?: string; affiliateUrl?: string; }
const _props = Astro.props as Record<string, unknown>;
void _props;
---
