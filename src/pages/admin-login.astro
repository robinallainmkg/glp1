---
// Page de connexion pour le dashboard admin
import BaseLayout from '../layouts/BaseLayout.astro';

// Vérifier si l'utilisateur est déjà connecté (côté client)
// Cette vérification sera faite en JavaScript

// Variable pour compatibilité template
const errorMessage = '';
---

<BaseLayout 
  title="Connexion Admin - GLP-1 France"
  description="Accès administrateur au dashboard de gestion"
  keywords="admin, connexion, authentification"
>

<div class="container">
  <div class="admin-header">
    <div class="admin-logo">
      <div class="logo-icon">🩺</div>
      <div class="logo-text">
        <h1>GLP-1 France</h1>
        <span class="logo-subtitle">Administration</span>
      </div>
    </div>
    <div class="admin-meta">
      <div class="system-status">
        <span class="status-indicator online"></span>
        <span>Système en ligne</span>
      </div>
    </div>
  </div>
  
  <div class="login-container">
    <div class="login-card">
      <div class="login-header">
        <div class="login-icon">🔐</div>
        <h2>Connexion Sécurisée</h2>
        <p class="login-subtitle">Accédez au tableau de bord administrateur</p>
      </div>
      
      {errorMessage && (
        <div class="error-message">
          ⚠️ {errorMessage}
        </div>
      )}
      
      <form id="loginForm" class="login-form">
        <div class="form-group">
          <label for="password">
            <span class="label-icon">🔑</span>
            Mot de passe administrateur
          </label>
          <div class="input-wrapper">
            <input 
              type="password" 
              id="password" 
              name="password" 
              required 
              placeholder="Entrez votre mot de passe"
              autocomplete="current-password"
            />
            <button type="button" class="show-password" id="togglePassword">
              👁️
            </button>
          </div>
        </div>
        
        <button type="submit" class="login-btn">
          <span class="btn-icon">🚀</span>
          Accéder au Dashboard
        </button>
      </form>
      
      <div id="errorDisplay" class="error-message" style="display: none;">
        ⚠️ <span id="errorText"></span>
      </div>
      
      <div class="login-info">
        <p>💡 <strong>Mot de passe de test:</strong> <code>glp1admin2024</code></p>
        <p>En production, cette authentification devrait être renforcée.</p>
      </div>
    </div>
  </div>
</div>

<style>
/* Styles pour la page de connexion admin moderne */
.container {
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  display: flex;
  flex-direction: column;
  position: relative;
  overflow: hidden;
}

.container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%23ffffff' fill-opacity='0.03'%3E%3Cpath d='M0 0h40v40H0V0zm10 10h20v20H10V10z'/%3E%3C/g%3E%3C/svg%3E") repeat;
  pointer-events: none;
}

/* Header Admin Moderne */
.admin-header {
  position: relative;
  z-index: 2;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.2);
  padding: var(--spacing-6) var(--spacing-8);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.admin-logo {
  display: flex;
  align-items: center;
  gap: var(--spacing-4);
}

.logo-icon {
  width: 60px;
  height: 60px;
  background: linear-gradient(135deg, var(--primary-blue), var(--accent-green));
  border-radius: var(--radius-xl);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 2rem;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
}

.logo-text h1 {
  color: white;
  font-size: var(--font-size-2xl);
  font-weight: 700;
  margin: 0;
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
}

.logo-subtitle {
  color: rgba(255, 255, 255, 0.8);
  font-size: var(--font-size-sm);
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.admin-meta {
  display: flex;
  align-items: center;
  gap: var(--spacing-4);
}

.system-status {
  display: flex;
  align-items: center;
  gap: var(--spacing-2);
  background: rgba(255, 255, 255, 0.1);
  padding: var(--spacing-2) var(--spacing-4);
  border-radius: var(--radius-full);
  color: white;
  font-size: var(--font-size-sm);
  backdrop-filter: blur(10px);
}

.status-indicator {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #22c55e;
  box-shadow: 0 0 10px #22c55e;
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.6; }
}

/* Container de connexion */
.login-container {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-8);
  position: relative;
  z-index: 2;
}

.login-card {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 2rem;
  padding: var(--spacing-12);
  width: 100%;
  max-width: 450px;
  box-shadow: 0 25px 50px rgba(0, 0, 0, 0.25);
  position: relative;
  overflow: hidden;
  animation: slideInUp 0.6s ease-out;
}

.login-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, var(--primary-blue), var(--accent-green));
}

.login-header {
  text-align: center;
  margin-bottom: var(--spacing-8);
}

.login-icon {
  width: 80px;
  height: 80px;
  background: linear-gradient(135deg, var(--primary-blue), var(--accent-green));
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 2.5rem;
  margin: 0 auto var(--spacing-4);
  box-shadow: 0 15px 35px rgba(37, 99, 235, 0.3);
}

.login-header h2 {
  color: var(--gray-900);
  font-size: var(--font-size-3xl);
  font-weight: 700;
  margin-bottom: var(--spacing-2);
}

.login-subtitle {
  color: var(--gray-600);
  font-size: var(--font-size-base);
  margin: 0;
}

/* Formulaire */
.login-form {
  margin-bottom: var(--spacing-6);
}

.form-group {
  margin-bottom: var(--spacing-6);
}

.form-group label {
  display: flex;
  align-items: center;
  gap: var(--spacing-2);
  margin-bottom: var(--spacing-3);
  font-weight: 600;
  color: var(--gray-700);
  font-size: var(--font-size-base);
}

.label-icon {
  font-size: var(--font-size-lg);
}

.input-wrapper {
  position: relative;
}

.form-group input {
  width: 100%;
  padding: var(--spacing-4) var(--spacing-6);
  padding-right: var(--spacing-12);
  border: 2px solid var(--gray-200);
  border-radius: var(--radius-lg);
  font-size: var(--font-size-base);
  transition: all 0.3s ease;
  background: white;
}

.form-group input:focus {
  outline: none;
  border-color: var(--primary-blue);
  box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.1);
  transform: translateY(-1px);
}

.show-password {
  position: absolute;
  right: var(--spacing-4);
  top: 50%;
  transform: translateY(-50%);
  background: none;
  border: none;
  cursor: pointer;
  font-size: var(--font-size-lg);
  padding: var(--spacing-2);
  border-radius: var(--radius-md);
  transition: background 0.3s ease;
}

.show-password:hover {
  background: var(--gray-100);
}

.login-btn {
  width: 100%;
  background: linear-gradient(135deg, var(--primary-blue), var(--accent-green));
  color: white;
  border: none;
  padding: var(--spacing-4) var(--spacing-6);
  border-radius: var(--radius-lg);
  font-size: var(--font-size-lg);
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-2);
  box-shadow: 0 10px 25px rgba(37, 99, 235, 0.3);
}

.login-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 15px 35px rgba(37, 99, 235, 0.4);
}

.login-btn:active {
  transform: translateY(0);
}

.btn-icon {
  font-size: var(--font-size-xl);
}

/* Messages d'erreur */
.error-message {
  background: linear-gradient(135deg, #fef2f2, #fecaca);
  border: 1px solid #fca5a5;
  color: #dc2626;
  padding: var(--spacing-4);
  border-radius: var(--radius-lg);
  margin-bottom: var(--spacing-4);
  display: flex;
  align-items: center;
  gap: var(--spacing-2);
  font-weight: 500;
}

.login-info {
  margin-top: var(--spacing-8);
  padding-top: var(--spacing-6);
  border-top: 1px solid var(--gray-200);
  font-size: var(--font-size-sm);
  color: var(--gray-600);
}

.login-info code {
  background: var(--gray-100);
  padding: var(--spacing-1) var(--spacing-2);
  border-radius: var(--radius-sm);
  font-family: 'Monaco', 'Consolas', monospace;
  color: var(--primary-blue);
}

/* Responsive */
@media (max-width: 768px) {
  .admin-header {
    padding: var(--spacing-4);
    flex-direction: column;
    gap: var(--spacing-4);
    text-align: center;
  }
  
  .admin-logo {
    flex-direction: column;
    text-align: center;
  }
  
  .logo-icon {
    width: 50px;
    height: 50px;
    font-size: 1.5rem;
  }
  
  .login-container {
    padding: var(--spacing-4);
  }
  
  .login-card {
    padding: var(--spacing-8);
    border-radius: 1.5rem;
  }
  
  .login-icon {
    width: 60px;
    height: 60px;
    font-size: 2rem;
  }
  
  .login-header h2 {
    font-size: var(--font-size-2xl);
  }
}

/* Animation d'entrée */
@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.admin-header {
  animation: slideInUp 0.4s ease-out;
}
  justify-content: center;
  align-items: center;
  min-height: 70vh;
  padding: var(--spacing-8) 0;
}

.login-card {
  background: white;
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-xl);
  padding: var(--spacing-12);
  max-width: 400px;
  width: 100%;
}

.login-card h1 {
  text-align: center;
  margin-bottom: var(--spacing-4);
  color: var(--gray-800);
  font-size: var(--font-size-2xl);
}

.login-subtitle {
  text-align: center;
  color: var(--gray-600);
  margin-bottom: var(--spacing-8);
}

.error-message {
  background: #fee2e2;
  color: #dc2626;
  padding: var(--spacing-3);
  border-radius: var(--radius-md);
  margin-bottom: var(--spacing-6);
  border-left: 4px solid #dc2626;
}

.form-group {
  margin-bottom: var(--spacing-6);
}

.form-group label {
  display: block;
  margin-bottom: var(--spacing-2);
  font-weight: 600;
  color: var(--gray-700);
}

.form-group input {
  width: 100%;
  padding: var(--spacing-3);
  border: 2px solid var(--gray-300);
  border-radius: var(--radius-md);
  font-size: var(--font-size-base);
  transition: var(--transition);
}

.form-group input:focus {
  outline: none;
  border-color: var(--primary-blue);
  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
}

.login-btn {
  width: 100%;
  background: var(--primary-blue);
  color: white;
  border: none;
  padding: var(--spacing-4);
  border-radius: var(--radius-md);
  font-size: var(--font-size-base);
  font-weight: 600;
  cursor: pointer;
  transition: var(--transition);
}

.login-btn:hover {
  background: var(--primary-blue-dark);
  transform: translateY(-1px);
}

.login-info {
  margin-top: var(--spacing-8);
  padding-top: var(--spacing-6);
  border-top: 1px solid var(--gray-200);
  font-size: var(--font-size-sm);
  color: var(--gray-600);
}

.login-info code {
  background: var(--gray-100);
  padding: var(--spacing-1);
  border-radius: var(--radius-sm);
  font-family: monospace;
}
</style>

<script>
<script>
// Authentification simple et fonctionnalités modernes
document.addEventListener('DOMContentLoaded', function() {
  const form = document.getElementById('loginForm');
  const passwordInput = document.getElementById('password');
  const togglePassword = document.getElementById('togglePassword');
  const errorDisplay = document.getElementById('errorDisplay');
  const errorText = document.getElementById('errorText');
  
  // Vérifier si l'utilisateur est déjà connecté
  const isAuthenticated = localStorage.getItem('admin_session') === 'authenticated';
  
  if (isAuthenticated) {
    window.location.href = '/admin/';
    return;
  }
  
  // Mot de passe correct (en production, utiliser une authentification sécurisée)
  const correctPassword = 'glp1admin2024';
  
  // Toggle de visibilité du mot de passe
  togglePassword?.addEventListener('click', function() {
    const type = passwordInput.type === 'password' ? 'text' : 'password';
    passwordInput.type = type;
    togglePassword.textContent = type === 'password' ? '👁️' : '🙈';
  });
  
  // Soumission du formulaire
  form?.addEventListener('submit', function(e) {
    e.preventDefault();
    
    const password = passwordInput.value;
    
    if (password === correctPassword) {
      // Authentification réussie
      localStorage.setItem('admin_session', 'authenticated');
      localStorage.setItem('admin_login_time', new Date().getTime().toString());
      
      // Animation de succès
      const loginBtn = form.querySelector('.login-btn');
      loginBtn.innerHTML = '<span class="btn-icon">✅</span> Connexion réussie...';
      loginBtn.style.background = 'linear-gradient(135deg, #22c55e, #16a34a)';
      
      // Redirection avec délai
      setTimeout(() => {
        window.location.href = '/admin/';
      }, 1000);
    } else {
      // Erreur d'authentification
      errorText.textContent = 'Mot de passe incorrect';
      errorDisplay.style.display = 'flex';
      
      // Animation de secousse
      const loginCard = document.querySelector('.login-card');
      loginCard.style.animation = 'shake 0.5s ease-in-out';
      setTimeout(() => {
        loginCard.style.animation = 'slideInUp 0.6s ease-out';
      }, 500);
      
      // Focus sur le champ
      passwordInput.value = '';
      passwordInput.focus();
    }
  });
  
  // Masquer l'erreur quand l'utilisateur tape
  passwordInput.addEventListener('input', function() {
    errorDisplay.style.display = 'none';
  });
  
  // Animations d'entrée pour les éléments
  setTimeout(() => {
    const elements = document.querySelectorAll('.form-group, .login-btn');
    elements.forEach((el, index) => {
      el.style.opacity = '0';
      el.style.transform = 'translateY(20px)';
      el.style.transition = 'all 0.4s ease';
      
      setTimeout(() => {
        el.style.opacity = '1';
        el.style.transform = 'translateY(0)';
      }, 200 + (index * 100));
    });
  }, 600);
});

// Animation de secousse pour les erreurs
const style = document.createElement('style');
style.textContent = `
  @keyframes shake {
    0%, 20%, 40%, 60%, 80% {
      transform: translateX(0);
    }
    10%, 30%, 50%, 70%, 90% {
      transform: translateX(-5px);
    }
  }
`;
document.head.appendChild(style);
</script>
</script>

</BaseLayout>
