---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ArticleCard from '../../../components/ArticleCard.astro';
import { getCollection } from 'astro:content';

// Récupérer tous les articles de cette collection
const articles = await getCollection('medecins-glp1-france');

// Configuration des couleurs
const colors = {
  bg: 'slate',
  text: 'slate-800',
  badge: 'slate-100'
};
---

<BaseLayout 
  title="Médecins en France - Guide complet des GLP-1"
  description="Trouver un médecin spécialisé en GLP-1 en France"
  keywords="medecins glp1-france, glp1, médecins en france"
>

<div class="container">
  <div class="max-w-6xl mx-auto">
    
    <!-- Hero Section -->
    <div class="bg-gradient-to-r from-slate-600 to-slate-700 text-white rounded-2xl p-8 mb-8">
      <div class="flex items-center gap-4 mb-4">
        <div class="text-4xl">👨‍⚕️</div>
        <h1 class="text-4xl font-bold">Médecins en France</h1>
      </div>
      <p class="text-xl opacity-90 mb-4">
        Trouver un médecin spécialisé en GLP-1 en France
      </p>
      <div class="bg-white/20 rounded-lg p-4 inline-block">
        <span class="font-semibold">{articles.length} articles disponibles</span>
      </div>
    </div>

    <!-- Articles Section -->
    <section class="collection-articles">
      <div class="mb-8">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Médecins en France</h2>
        <p class="text-lg text-gray-600">
          Trouver un médecin spécialisé en GLP-1 en France
        </p>
      </div>
      
      <!-- Grille des articles avec images -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {articles.map((article) => (
          <ArticleCard 
            article={article}
            collection="medecins-glp1-france"
            categoryName="Médecins en France"
            colors={colors}
          />
        ))}
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-12 bg-gradient-to-r from-slate-50 to-slate-100 rounded-2xl mt-12">
      <div class="text-center px-8">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">
          Besoin d'informations personnalisées ?
        </h2>
        <p class="text-lg text-gray-700 mb-8">
          Consultez notre guide complet et téléchargez nos ressources gratuites.
        </p>
        <a 
          href="/guide-beaute-perte-de-poids-glp1/" 
          class="inline-flex items-center px-8 py-4 bg-slate-600 text-white font-bold rounded-xl hover:bg-slate-700 transition-all duration-300 transform hover:scale-105 shadow-xl"
        >
          <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
          Télécharger le guide gratuit
        </a>
      </div>
    </section>
  </div>
</div>

</BaseLayout>