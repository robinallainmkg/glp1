---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ArticleCard from '../../../components/ArticleCard.astro';
import { getCollection } from 'astro:content';

// Récupérer tous les articles de cette collection
const articles = await getCollection('medicaments-glp1');

// Configuration des couleurs
const colors = {
  bg: 'green',
  text: 'green-800',
  badge: 'green-100'
};
---

<BaseLayout 
  title="Médicaments GLP-1 - Guide complet des traitements"
  description="Guide complet sur les médicaments GLP-1 : Ozempic, Wegovy, Saxenda, Trulicity et autres agonistes du récepteur GLP-1"
  keywords="médicaments glp1, ozempic, wegovy, saxenda, trulicity, agonistes glp1"
>

<div class="container">
  <div class="max-w-6xl mx-auto">
    
    <!-- Hero Section -->
    <div class="bg-gradient-to-r from-green-600 to-green-700 text-white rounded-2xl p-8 mb-8">
      <div class="flex items-center gap-4 mb-4">
        <div class="text-4xl">💊</div>
        <h1 class="text-4xl font-bold">Médicaments GLP-1</h1>
      </div>
      <p class="text-xl opacity-90 mb-4">
        Guide complet sur les traitements GLP-1 disponibles en France
      </p>
      <div class="bg-white/20 rounded-lg p-4 inline-block">
        <span class="font-semibold">{articles.length} articles disponibles</span>
      </div>
    </div>

    <!-- Articles Section -->
    <section class="collection-articles">
      <div class="mb-8">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Médicaments GLP-1</h2>
        <p class="text-lg text-gray-600">
          Découvrez tous les médicaments GLP-1 : indications, posologies, effets secondaires et comparaisons
        </p>
      </div>
      
      <!-- Grille des articles avec images -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {articles.map((article) => (
          <ArticleCard 
            article={article}
            collection="medicaments-glp1"
            categoryName="Médicaments GLP-1"
            colors={colors}
          />
        ))}
      </div>
    </section>

  </div>
</div>

</BaseLayout>

<style>
  .collection-articles {
    margin-bottom: 3rem;
  }
  
  .container {
    padding: 2rem 1rem;
  }
  
  @media (max-width: 768px) {
    .container {
      padding: 1rem;
    }
    
    .bg-gradient-to-r {
      padding: 1.5rem;
    }
    
    h1 {
      font-size: 2rem;
    }
  }
</style>
