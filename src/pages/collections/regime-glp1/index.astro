---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ArticleCard from '../../../components/ArticleCard.astro';
import { getCollection } from 'astro:content';

// Récupérer tous les articles de cette collection
const articles = await getCollection('regime-glp1');

// Configuration des couleurs
const colors = {
  bg: 'orange',
  text: 'orange-800',
  badge: 'orange-100'
};
---

<BaseLayout 
  title="Régime & Nutrition - Guide complet des GLP-1"
  description="Régimes alimentaires adaptés aux traitements GLP-1"
  keywords="regime glp1, glp1, régime & nutrition"
>

<div class="container">
  <div class="max-w-6xl mx-auto">
    
    <!-- Hero Section -->
    <div class="bg-gradient-to-r from-orange-600 to-orange-700 text-white rounded-2xl p-8 mb-8">
      <div class="flex items-center gap-4 mb-4">
        <div class="text-4xl">🥗</div>
        <h1 class="text-4xl font-bold">Régime & Nutrition</h1>
      </div>
      <p class="text-xl opacity-90 mb-4">
        Régimes alimentaires adaptés aux traitements GLP-1
      </p>
      <div class="bg-white/20 rounded-lg p-4 inline-block">
        <span class="font-semibold">{articles.length} articles disponibles</span>
      </div>
    </div>

    <!-- Articles Section -->
    <section class="collection-articles">
      <div class="mb-8">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Régime & Nutrition</h2>
        <p class="text-lg text-gray-600">
          Régimes alimentaires adaptés aux traitements GLP-1
        </p>
      </div>
      
      <!-- Grille des articles avec images -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {articles.map((article) => (
          <ArticleCard 
            article={article}
            collection="regime-glp1"
            categoryName="Régime & Nutrition"
            colors={colors}
          />
        ))}
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-12 bg-gradient-to-r from-orange-50 to-orange-100 rounded-2xl mt-12">
      <div class="text-center px-8">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">
          Besoin d'informations personnalisées ?
        </h2>
        <p class="text-lg text-gray-700 mb-8">
          Consultez notre guide complet et téléchargez nos ressources gratuites.
        </p>
        <a 
          href="/contact" 
          class="inline-flex items-center px-8 py-4 bg-blue-600 text-white font-bold rounded-xl hover:bg-blue-700 transition-all duration-300 transform hover:scale-105 shadow-xl"
        >
          <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
          </svg>
          Nous Contacter
        </a>
      </div>
    </section>
  </div>
</div>

</BaseLayout>