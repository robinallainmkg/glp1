---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import AdminNav from '../components/AdminNav.astro';
import pertinenceReport from '../../pertinence-content-report.json';

// === DONNÉES STATISTIQUES ===
function countWords(text) {
  if (!text) return 0;
  return text
    .replace(/`{3}[\s\S]*?`{3}/g, ' ')
    .replace(/`[^`]*`/g, ' ')
    .replace(/<[^>]+>/g, ' ')
    .split(/\s+/)
    .filter(Boolean)
    .length;
}

const articles = await getCollection('blog');
const articlesData = articles.map(article => {
  const md = article.body || '';
  const wordCount = countWords(md);
  
  return {
    slug: article.slug,
    title: article.data.title,
    category: article.data.category,
    wordCount,
    isEmpty: wordCount < 100,
    hasH1: md.includes('# '),
    lastModified: article.data.lastModified || article.data.pubDate
  };
});

const totalArticles = articlesData.length;
const emptyArticles = articlesData.filter(s => s.isEmpty).length;
const lastUpdate = new Date(pertinenceReport.timestamp).toLocaleString('fr-FR', { dateStyle: 'long', timeStyle: 'short' });

const title = "Dashboard Admin - GLP-1 France";
const description = "Dashboard de suivi SEO et monétisation du site GLP-1 France";
---

<BaseLayout title={title} description={description}>
  <AdminNav />
  
  <div class="min-h-screen bg-gray-50 py-8">
    <div class="container mx-auto px-4 max-w-7xl">
      
      <!-- Header -->
      <div class="dashboard-header bg-gradient-to-r from-indigo-600 to-purple-600 text-white p-8 rounded-xl text-center mb-8 shadow-lg">
        <h1 class="text-4xl font-bold mb-2">🏥 Dashboard Admin - GLP-1 France</h1>
        <p class="text-lg opacity-90">Monitoring complet • SEO • Monétisation</p>
        <p class="text-sm mt-2 text-yellow-200">Dernière mise à jour : {lastUpdate}</p>
      </div>

      <!-- Stats Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-blue-500">
          <div class="flex items-center">
            <div class="p-3 bg-blue-100 rounded-full">
              <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
            </div>
            <div class="ml-4">
              <p class="text-3xl font-bold text-gray-900">{totalArticles}</p>
              <p class="text-sm font-medium text-gray-600">Articles total</p>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-red-500">
          <div class="flex items-center">
            <div class="p-3 bg-red-100 rounded-full">
              <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.732 15.5c-.77.833.192 2.5 1.732 2.5z"></path>
              </svg>
            </div>
            <div class="ml-4">
              <p class="text-3xl font-bold text-gray-900">{emptyArticles}</p>
              <p class="text-sm font-medium text-gray-600">Articles vides</p>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-green-500">
          <div class="flex items-center">
            <div class="p-3 bg-green-100 rounded-full">
              <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
              </svg>
            </div>
            <div class="ml-4">
              <p class="text-3xl font-bold text-gray-900">{Math.round(((totalArticles - emptyArticles) / totalArticles) * 100)}%</p>
              <p class="text-sm font-medium text-gray-600">Complétion</p>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-yellow-500">
          <div class="flex items-center">
            <div class="p-3 bg-yellow-100 rounded-full">
              <svg class="w-8 h-8 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
              </svg>
            </div>
            <div class="ml-4">
              <p class="text-3xl font-bold text-gray-900">{articlesData.filter(a => a.wordCount > 1000).length}</p>
              <p class="text-sm font-medium text-gray-600">Articles longs</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Quick Actions -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-8">
        <div class="bg-white rounded-lg shadow-md p-6">
          <h2 class="text-xl font-bold text-gray-800 mb-4">🚀 Actions Rapides</h2>
          <div class="space-y-3">
            <a href="/admin-user-data/" class="block w-full bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors text-center">
              👥 Voir les données utilisateurs
            </a>
            <a href="/admin-affiliation/" class="block w-full bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700 transition-colors text-center">
              💰 Gérer l'affiliation
            </a>
            <a href="/admin-stats/" class="block w-full bg-purple-600 text-white px-4 py-2 rounded-lg hover:bg-purple-700 transition-colors text-center">
              📈 Voir les statistiques
            </a>
          </div>
        </div>

        <div class="bg-white rounded-lg shadow-md p-6">
          <h2 class="text-xl font-bold text-gray-800 mb-4">📊 Résumé SEO</h2>
          <div class="space-y-3">
            <div class="flex justify-between items-center">
              <span class="text-gray-600">Articles complets:</span>
              <span class="font-semibold text-green-600">{totalArticles - emptyArticles}</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-gray-600">Articles à compléter:</span>
              <span class="font-semibold text-red-600">{emptyArticles}</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-gray-600">Articles longs (>1000 mots):</span>
              <span class="font-semibold text-blue-600">{articlesData.filter(a => a.wordCount > 1000).length}</span>
            </div>
          </div>
        </div>
      </div>

      <!-- Articles récents -->
      <div class="bg-white rounded-lg shadow-md p-6">
        <h2 class="text-xl font-bold text-gray-800 mb-4">📝 Articles récents</h2>
        <div class="overflow-x-auto">
          <table class="min-w-full table-auto">
            <thead>
              <tr class="bg-gray-50">
                <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Titre</th>
                <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Catégorie</th>
                <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Mots</th>
                <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Statut</th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              {articlesData.slice(0, 10).map(article => (
                <tr>
                  <td class="px-4 py-2 whitespace-nowrap">
                    <a href={`/${article.slug}/`} class="text-blue-600 hover:text-blue-800 font-medium">
                      {article.title}
                    </a>
                  </td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-600">
                    {article.category}
                  </td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-600">
                    {article.wordCount}
                  </td>
                  <td class="px-4 py-2 whitespace-nowrap">
                    {article.isEmpty ? (
                      <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-red-100 text-red-800">
                        Vide
                      </span>
                    ) : article.wordCount > 1000 ? (
                      <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-green-100 text-green-800">
                        Complet
                      </span>
                    ) : (
                      <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-yellow-100 text-yellow-800">
                        Moyen
                      </span>
                    )}
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>

    </div>
  </div>
</BaseLayout>

<style>
  .transition-colors {
    transition: all 0.2s ease;
  }
</style>
