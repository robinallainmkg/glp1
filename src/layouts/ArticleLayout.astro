---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  author: string;
  category: string;
  keywords: string | string[];
  readingTime: number;
  lastModified?: string;
  content: string;
  collectionTheme?: string;
  thumbnail?: string;
}

const { 
  title, 
  description, 
  author, 
  category, 
  keywords, 
  readingTime, 
  lastModified,
  content,
  collectionTheme = 'weight-loss',
  thumbnail
} = Astro.props;

// Convertir les mots-clés en chaîne si c'est un tableau
const keywordsString = Array.isArray(keywords) ? keywords.join(', ') : keywords;

// Définir les couleurs selon la collection - Version neutre et professionnelle
const themeConfig = {
  'weight-loss': {
    gradient: 'from-slate-50 to-gray-100',
    textColor: 'text-slate-700',
    badge: 'bg-slate-100 text-slate-700 border border-slate-200',
    icon: '⚖️',
    accentColor: 'slate-600'
  },
  'medical': {
    gradient: 'from-slate-50 to-blue-50',
    textColor: 'text-slate-700',
    badge: 'bg-slate-100 text-slate-700 border border-slate-200', 
    icon: '💊',
    accentColor: 'slate-600'
  },
  'nutrition': {
    gradient: 'from-slate-50 to-orange-50',
    textColor: 'text-slate-700',
    badge: 'bg-slate-100 text-slate-700 border border-slate-200',
    icon: '🥗',
    accentColor: 'slate-600'
  },
  'research': {
    gradient: 'from-slate-50 to-indigo-50',
    textColor: 'text-slate-700',
    badge: 'bg-slate-100 text-slate-700 border border-slate-200',
    icon: '🔬',
    accentColor: 'slate-600'
  },
  'cost': {
    gradient: 'from-slate-50 to-rose-50',
    textColor: 'text-slate-700',
    badge: 'bg-slate-100 text-slate-700 border border-slate-200',
    icon: '💰',
    accentColor: 'slate-600'
  },
  'side-effects': {
    gradient: 'from-slate-50 to-amber-50',
    textColor: 'text-slate-700',
    badge: 'bg-slate-100 text-slate-700 border border-slate-200',
    icon: '⚠️',
    accentColor: 'slate-600'
  },
  'diabetes': {
    gradient: 'from-slate-50 to-violet-50',
    textColor: 'text-slate-700',
    badge: 'bg-slate-100 text-slate-700 border border-slate-200',
    icon: '🩺',
    accentColor: 'slate-600'
  }
};

const theme = themeConfig[collectionTheme] || themeConfig['weight-loss'];

// Mapping des catégories vers des noms lisibles et URLs
const categoryMapping = {
  'glp1-perte-de-poids': { name: 'GLP-1 et Perte de Poids', url: '/collections/glp1-perte-de-poids' },
  'medicaments-glp1': { name: 'Médicaments GLP-1', url: '/collections/medicaments-glp1' },
  'glp1-cout': { name: 'Prix et Coûts GLP-1', url: '/collections/glp1-cout' },
  'effets-secondaires-glp1': { name: 'Effets Secondaires', url: '/collections/effets-secondaires-glp1' },
  'glp1-diabete': { name: 'GLP-1 et Diabète', url: '/collections/glp1-diabete' },
  'recherche-glp1': { name: 'Recherche GLP-1', url: '/collections/recherche-glp1' },
  'medecins-glp1-france': { name: 'Médecins GLP-1 France', url: '/collections/medecins-glp1-france' },
  'alternatives-glp1': { name: 'Alternatives GLP-1', url: '/collections/alternatives-glp1' },
  'regime-glp1': { name: 'Régime GLP-1', url: '/collections/regime-glp1' }
};

// Fonction pour générer le fil d'Ariane
function generateBreadcrumb(category, title) {
  const breadcrumb = [
    { name: 'Accueil', url: '/', icon: '🏠' }
  ];
  
  // Maintenant category contient toujours le nom exact de la collection
  if (categoryMapping[category]) {
    breadcrumb.push({
      name: categoryMapping[category].name,
      url: categoryMapping[category].url,
      icon: theme?.icon || '📄'
    });
  }
  
  // Article actuel (sans lien)
  breadcrumb.push({
    name: title.length > 50 ? title.substring(0, 50) + '...' : title,
    url: null,
    icon: '📖'
  });
  
  return breadcrumb;
}

// Fonction pour obtenir l'URL de retour correcte
function getBackUrl(category) {
  // Maintenant on a directement le nom de la collection
  if (categoryMapping[category]) {
    return categoryMapping[category].url;
  }
  
  // Fallback simple
  return `/${category}/`;
}

// Fonction pour obtenir le nom affiché de la collection  
function getDisplayName(category) {
  if (categoryMapping[category]) {
    return categoryMapping[category].name;
  }
  
  // Fallback: formatter la catégorie
  return category.replace(/-/g, ' ').replace(/\b\w/g, l => l.toUpperCase());
}

const breadcrumb = generateBreadcrumb(category, title);
const backUrl = getBackUrl(category);
const displayName = getDisplayName(category);

// Récupérer l'image depuis le frontmatter si elle existe
function getArticleImage(frontmatter, category, slug) {
  // Utiliser le champ thumbnail du frontmatter s'il existe
  if (frontmatter?.thumbnail) {
    return frontmatter.thumbnail;
  }
  
  // Ne pas générer d'URL par défaut - retourner null si pas d'image
  return null;
}

// Obtenir l'image de l'article  
const articleImage = getArticleImage({ thumbnail }, category, Astro.url.pathname.split('/').pop()?.replace('.html', '') || 'default');

// Conversion simple du markdown en HTML
function markdownToHtml(markdown: string): string {
  return markdown
    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
    .replace(/\*(.*?)\*/g, '<em>$1</em>')
    .replace(/^## (.*$)/gm, '<h2>$1</h2>')
    .replace(/^### (.*$)/gm, '<h3>$1</h3>')
    .replace(/^#### (.*$)/gm, '<h4>$1</h4>')
    .replace(/^\- (.*$)/gm, '<li>$1</li>')
    .replace(/(<li>.*<\/li>)/gs, '<ul>$1</ul>')
    .replace(/\[affiliate-box\]/g, '') // Supprimer les marqueurs d'affiliation
    .replace(/\r?\n\r?\n/g, '</p><p>')
    .replace(/^(?!<[h|u|l])/gm, '<p>')
    .replace(/(?<!>)$/gm, '</p>')
    .replace(/<p><\/p>/g, '')
    .replace(/<p>(<[h|u])/g, '$1')
    .replace(/(<\/[h|u][^>]*>)<\/p>/g, '$1');
}

// Conversion du contenu
const contentHtml = markdownToHtml(content);
---

<BaseLayout title={title} description={description}>
  <article class="article-layout">
    <!-- Fil d'Ariane -->
    <nav class="breadcrumb" style="background: #f8fafc; border-bottom: 1px solid #e2e8f0; padding: 12px 0;">
      <div style="max-width: 1200px; margin: 0 auto; padding: 0 16px;">
        <div style="display: flex; align-items: center; gap: 8px; font-size: 14px; flex-wrap: wrap;">
          {breadcrumb.map((item, index) => (
            <Fragment>
              {index > 0 && <span style="color: #9ca3af;">→</span>}
              {item.url ? (
                <a href={item.url} style="color: #3b82f6; text-decoration: none; display: flex; align-items: center; gap: 4px;">
                  <span>{item.icon}</span>
                  <span>{item.name}</span>
                </a>
              ) : (
                <span style="color: #6b7280; display: flex; align-items: center; gap: 4px;">
                  <span>{item.icon}</span>
                  <span>{item.name}</span>
                </span>
              )}
            </Fragment>
          ))}
        </div>
      </div>
    </nav>

    <!-- En-tête de l'article avec image -->
    <header class={`article-header bg-gradient-to-br ${theme.gradient} border-b border-gray-200`}>
      <div class="container mx-auto px-4 py-8">
        <div class="max-w-6xl mx-auto">
          <div class="grid lg:grid-cols-2 gap-8 items-center">
            <!-- Contenu textuel -->
            <div>
              <div class="flex items-center gap-4 mb-6 pt-6">
                <span class="text-3xl">{theme.icon}</span>
                <span class={`px-3 py-1 rounded-full text-sm font-medium ${theme.badge}`}>
                  {category.replace(/-/g, ' ').replace(/\b\w/g, l => l.toUpperCase())}
                </span>
              </div>
              
              <h1 class={`text-3xl md:text-4xl font-bold mb-4 leading-tight ${theme.textColor}`}>
                {title}
              </h1>
              
              <p class={`text-lg ${theme.textColor} opacity-90 mb-6 leading-relaxed`}>
                {description}
              </p>
              
              <div class={`flex flex-wrap items-center gap-6 text-sm ${theme.textColor} opacity-75`}>
                <div class="flex items-center gap-2">
                  <span class="text-base">✍️</span>
                  <span class="font-medium">{author}</span>
                </div>
                <div class="flex items-center gap-2">
                  <span class="text-base">⏱️</span>
                  <span>{readingTime} min de lecture</span>
                </div>
                {lastModified && (
                  <div class="flex items-center gap-2">
                    <span class="text-base">📅</span>
                    <span>{new Date(lastModified).toLocaleDateString('fr-FR')}</span>
                  </div>
                )}
              </div>
            </div>
            
            <!-- Image de l'article -->
            {articleImage && (
              <div class="lg:order-2">
                <div class="relative">
                  <img 
                    src={articleImage} 
                    alt={title}
                    class="w-full h-64 lg:h-80 object-cover rounded-xl shadow-lg"
                    loading="eager"
                  />
                  <!-- Overlay avec gradient pour améliorer la lisibilité -->
                  <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent rounded-xl"></div>
                </div>
              </div>
            )}
          </div>
        </div>
      </div>
    </header>

    <!-- Contenu de l'article -->
    <main class="article-content">
      <div class="container mx-auto px-4 py-12">
        <div class="max-w-4xl mx-auto">
          
          <!-- Contenu principal -->
          <div class="prose prose-lg max-w-none">
            <div set:html={contentHtml}></div>
          </div>
          
          <!-- Zone de mots-clés -->
          {keywordsString && (
            <div class="keywords-section mt-12 p-6 bg-gray-50 rounded-xl">
              <h3 class="text-lg font-semibold mb-4">🏷️ Mots-clés</h3>
              <div class="flex flex-wrap gap-2">
                {keywordsString.split(',').map(keyword => (
                  <span class="px-3 py-1 bg-white rounded-full text-sm text-gray-700 border">
                    {keyword.trim()}
                  </span>
                ))}
              </div>
            </div>
          )}
          
          <!-- Navigation de retour -->
          <nav class="article-navigation mt-12">
            <a 
              href={backUrl} 
              class={`inline-flex items-center gap-2 px-6 py-3 bg-gradient-to-r from-slate-600 to-slate-700 text-white rounded-lg hover:shadow-lg transition-all duration-300`}
            >
              ← Retour à {displayName}
            </a>
          </nav>
        </div>
      </div>
    </main>
  </article>
</BaseLayout>

<style>
  .article-layout {
    min-height: 100vh;
  }

  .article-header {
    position: relative;
    overflow: hidden;
  }

  .article-header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 100%);
    pointer-events: none;
  }

  .prose {
    color: #374151;
    line-height: 1.8;
  }

  .prose h2 {
    color: #1f2937;
    font-size: 1.875rem;
    font-weight: 700;
    margin: 2rem 0 1rem 0;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid #e5e7eb;
  }

  .prose h3 {
    color: #374151;
    font-size: 1.5rem;
    font-weight: 600;
    margin: 1.5rem 0 0.75rem 0;
  }

  .prose h4 {
    color: #4b5563;
    font-size: 1.25rem;
    font-weight: 600;
    margin: 1.25rem 0 0.5rem 0;
  }

  .prose p {
    margin: 1rem 0;
    line-height: 1.8;
  }

  .prose ul {
    margin: 1rem 0;
    padding-left: 0;
    list-style: none;
  }

  .prose li {
    margin: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
  }

  .prose ul li::before {
    content: "✓";
    color: #16A34A;
    font-weight: bold;
    position: absolute;
    left: 0;
    top: 0;
  }

  .prose li[data-has-symbol]::before {
    content: none;
  }

  .prose strong {
    color: #1f2937;
    font-weight: 600;
  }

  .prose em {
    font-style: italic;
    color: #6b7280;
  }

  .affiliate-box {
    background: linear-gradient(135deg, #fef3c7 0%, #fcd34d 100%);
    border: 2px solid #f59e0b;
    border-radius: 1rem;
    padding: 1.5rem;
    margin: 2rem 0;
    text-align: center;
    font-weight: 600;
    color: #92400e;
  }

  .keywords-section {
    border-left: 4px solid var(--primary-blue);
  }

  .article-navigation {
    border-top: 1px solid #e5e7eb;
    padding-top: 2rem;
  }

  /* Styles pour le fil d'Ariane */
  .breadcrumb {
    background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
    border-bottom: 1px solid #e2e8f0;
  }

  .breadcrumb ol {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .breadcrumb li {
    display: inline-flex;
    align-items: center;
  }

  .breadcrumb a {
    text-decoration: none;
    transition: all 0.2s ease;
    border-radius: 4px;
    padding: 2px 6px;
  }

  .breadcrumb a:hover {
    background-color: rgba(59, 130, 246, 0.1);
    transform: translateY(-1px);
  }

  .breadcrumb svg {
    flex-shrink: 0;
  }

  @media (max-width: 768px) {
    .breadcrumb {
      padding: 0.5rem 0;
    }

    .breadcrumb ol {
      flex-wrap: wrap;
      gap: 0.25rem;
    }

    .breadcrumb span {
      font-size: 0.875rem;
    }

    .breadcrumb svg {
      width: 0.875rem;
      height: 0.875rem;
    }

    .article-header {
      padding: 2rem 0;
    }
    
    .article-header h1 {
      font-size: 2rem;
    }
    
    .article-content {
      padding: 2rem 0;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Gestion des symboles doubles dans les listes
    const listItems = document.querySelectorAll('.prose li');
    listItems.forEach(li => {
      const text = li.textContent.trim();
      // Vérifie si l'élément contient déjà des symboles
      if (text.includes('→') || text.includes('✓') || text.includes('•') || text.includes('▸') || text.includes('-')) {
        li.setAttribute('data-has-symbol', 'true');
      }
    });
  });
</script>
